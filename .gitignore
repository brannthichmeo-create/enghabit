node_modules/
dist/
build/
.turbo/

# Env — KHÔNG BAO GIỜ commit file .env thật (xem CLAUDE.md > Quy chuẩn kết nối database)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code — settings.json dùng chung cả nhóm nên vẫn commit,
# chỉ bỏ qua cấu hình cá nhân của từng người
.claude/settings.local.json

# Dự án dùng pnpm — chặn lockfile của npm/yarn lọt vào gây xung đột
package-lock.json
yarn.lock

# Build tool caches
.vite/
coverage/
*.tsbuildinfo

# Expo (mobile)
.expo/
